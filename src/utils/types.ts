export enum Languages {
    en = "en",
    de = "de",
}

export enum Themes {
    dark = "dark",
    light = "light",
}

export enum Units {
    metric = "metric",
    imperial = "imperial",
}

export enum SwitchButtonTypes {
    lang = "lang",
    theme = "theme",
    unit = "unit",
}
